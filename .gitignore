# Secrets — never commit real credentials. Copy .env.example to .env and fill it in.
.env

# Runtime state — per-user ticket data (state, notes, transcripts), regenerated on use.
state/

# Python
__pycache__/
*.pyc
scripts/.venv/
*.egg-info/

# Your own company/repo names for the re-coupling guard test (see tests/test_projects.py)
scripts/tests/.forbidden-words
