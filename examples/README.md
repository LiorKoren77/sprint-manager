# Example project profiles

Copy one to `~/.config/sprint-manager/projects/<name>/` (personal) or to `<repo>/.sprint-manager/`
(committed with the repo), then set `repo`. The full reference is in `docs/projects.md`.
